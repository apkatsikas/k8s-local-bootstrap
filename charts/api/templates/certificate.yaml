apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-tls
  namespace: {{ .Values.namespace }}
spec:
  secretName: api-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.hostname }}
